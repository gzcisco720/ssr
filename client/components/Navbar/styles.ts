import { createStyles } from '@material-ui/styles';

export default createStyles({
  root: {
    width: '100%',
  },
  mainTitle: {
    flex: 1,
    textAlign: 'center',
    textTransform: 'uppercase',
    marginRight: '-6em',
  },
});
