import { createStyles } from '@material-ui/styles';

export default createStyles({
  root: {
    backgroundColor: 'white',
    padding: '20px',
  },
  title: {
    marginBottom: '20px',
  },
  selectItem: {
    display: 'inline-flex',
    alignItems: 'center',
  },
  replyButton: {
    position: 'fixed',
    right: 30,
    bottom: 20,
  },
});
